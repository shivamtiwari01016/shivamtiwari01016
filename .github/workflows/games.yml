name: Generate GitHub Profile Games

on:
  schedule:
    - cron: "0 0 * * *" # Runs every night
  workflow_dispatch: # Allows manual trigger

jobs:
  generate:
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    steps:
      - name: generate-game-assets
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: shivamtiwari01016
          outputs: |
            # 1. PAC-MAN Style
            dist/pacman.svg?palette=github-dark&color_snake=yellow&color_dots=#bfd6f6
            
            # 2. TETRIS Style
            dist/tetris.svg?palette=github-light&color_snake=orange
            
            # 3. CHARACTER WALKING (RPG Style)
            dist/rpg-walk.svg?palette=github-dark&color_snake=#3c7dd9
            
            # 4. MATRIX Style
            dist/matrix.svg?palette=github-dark&color_snake=#00FF00&color_dots=#003300
            
            # 5. BATTLEFIELD (City Strike)
            dist/battlefield-strike.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: push-to-output-branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
